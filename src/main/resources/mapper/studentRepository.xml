<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="raisetech.Student.Management.repository.StudentRepository" >

  <!-- 受講生の全件検索 -->
  <select id="studentListSearch" resultType="raisetech.Student.Management.data.Student">
    SELECT * FROM students
  </select>

  <!-- 受講生の検索 -->
  <select id="studentSearch" resultType="raisetech.Student.Management.data.Student">
    SELECT * FROM students WHERE student_no = #{studentNo}
  </select>


  <select id="studentCourseListSearch" resultType="raisetech.Student.Management.data.StudentCourse">
    SELECT * FROM students_courses
  </select>


  <select id="studentCourseSearch" resultType="raisetech.Student.Management.data.StudentCourse">
    SELECT * FROM students_courses WHERE student_no = #{studentNo}
  </select>

  <insert id="registerStudent" useGeneratedKeys="true" keyProperty="studentNo" keyColumn="student_no">
    INSERT INTO students
    (student_name, furigana, nickname, email, city, age, gender, remark, is_deleted)
    VALUES
    (#{studentName}, #{furigana}, #{nickname}, #{email}, #{city}, #{age}, #{gender}, #{remark}, false)
  </insert>

  <insert id="registerStudentCourse">
    INSERT INTO students_courses
    (id, student_no, course_name, start_date, end_date)
    VALUES
    (#{id}, #{studentNo}, #{courseName}, #{startDate}, #{endDate})
  </insert>

  <update id="updateStudent">
    UPDATE students SET student_name = #{studentName},furigana = #{furigana},nickname = #{nickname},
    email =#{email},city = #{city},age = #{age},gender = #{gender},remark = #{remark},is_deleted = #{isDeleted} WHERE student_no = #{studentNo}
  </update>

  <update id="updateStudentCourse">
    UPDATE students_courses SET course_name = #{courseName} WHERE id = #{id}
  </update>
</mapper>